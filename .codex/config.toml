# Repo-local Codex config intentionally does not define MCP servers.
# Shared MCP source of truth lives in:
# - D:\Work\mcp\registry.template.json
# - D:\Work\scripts\sync-mcp-config.ps1
#
# Keeping this file empty avoids stale project-level MCP overrides from
# shadowing the managed user-level config in %USERPROFILE%\.codex\config.toml.
