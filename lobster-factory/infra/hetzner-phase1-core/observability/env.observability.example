# Copy to observability/.env.observability (do not commit real passwords).
# OBSERVABILITY_HOST_LABEL appears as label "host" in Loki.
OBSERVABILITY_HOST_LABEL=hetzner-phase1-vps

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-immediately
GRAFANA_ROOT_URL=http://127.0.0.1:3009
