---
description: Resume session using AO-RESUME keyword
alwaysApply: true
---

# AO Resume Keyword

**操作者硬性要求（已定調；預設體驗）**：使用者只打 **`AO-RESUME`** 即表示「**凡本機 monorepo 內可由 IDE hook、代理＋腳本自動完成的事，一律自動做**」，**不要**把可一次跑完的步驟改寫成「請你先手動 pull／請你先開終端貼指令」才把對話接下去。**送出訊息當下**，本 repo 之 **`.cursor/hooks.json`** 已盡力先跑 **`ao-resume.ps1 -FullMainlineParity`**（見第 1 點「Cursor 專案 hook」）。其後：若依「hook 去重」**無須**再跑，代理**不得**為了形式再開終端重跑；若仍須跑（無 hook 成品／過期／無終端等），具 **Shell** 且工作區可解析到 **repo 根**時，代理**必須**執行 **`ao-resume.ps1 -FullMainlineParity`** 再產出第 3 步結構化回覆。**只有**（a）環境**完全無終端**、（b）`exit≠0` 且需人類決策（合併策略、刪檔、憑證輸入）、或（c）工作發生在**另一台實體電腦**（見第 6、7 點）時，才用最少文字說明卡點；仍**禁止**要求使用者重複做「hook 或代理剛剛若自跑即可成功」的同一輪動作。

When the user message contains `AO-RESUME`, before proposing any new implementation:

1. **開工單一路徑（關鍵字 `AO-RESUME`＝跑正本腳本）**  
   - **Cursor 專案 hook（會真的執行）**：本 monorepo 根目錄 **`.cursor/hooks.json`** 已註冊 **`beforeSubmitPrompt`** → **`.cursor/hooks/run-ao-resume-on-prompt.ps1`**。使用者按送出且訊息符合 **`^AO-RESUME\b`**（不分大小寫）時，**Cursor 本機**會先檢查 working tree；若 dirty 則先自動跑 **`scripts\commit-checkpoint.ps1`** 做本機 checkpoint，再執行 **`scripts\ao-resume.ps1 -FullMainlineParity`**，並寫入 **`agency-os/.agency-state/ao-resume-hook-last.json`**（含 **`exit_code`**、**`log_tail`**、`auto_checkpoint_*`）與 **`ao-resume-hook-last.log`**（皆 gitignore）。**這不是「只改規則」**：腳本由 **IDE hook 行程**觸發。重載視窗或重啟 Cursor 後仍應生效；除錯見 Cursor **Hooks** 輸出通道。  
   - **與 hook 去重（避免同輪跑兩次）**：若本則使用者訊息仍為 **`AO-RESUME`**，代理**先 Read** **`agency-os/.agency-state/ao-resume-hook-last.json`**，並以 **`Get-Item`**（或等價）檢查該檔 **LastWriteTime 距今 ≤120 秒**；若成立且 JSON 內 **`exit_code`** 為整數，**視為本輪已由 hook 執行過 `ao-resume.ps1`**——**禁止**再開終端重跑同一指令；五段式之 **Git／開工裁決** 以該 JSON 之 **`exit_code`** 與 **`log_tail`**（及必要時 **Read** 完整 **`ao-resume-hook-last.log`**）為準。**若檔案不存在、逾 120 秒、或本則非 AO-RESUME**，不適用去重。  
   - **Agent 必做**：在可執行終端且**未**觸發上一條「hook 去重」時，於 **monorepo 根**（`git rev-parse --show-toplevel`）執行 **`powershell -ExecutionPolicy Bypass -File .\scripts\ao-resume.ps1 -FullMainlineParity`**（**必含** `-FullMainlineParity`：先 **`git-align-main-aoresume-feature.ps1 -SkipAoResume -SkipFeature`** 對齊本機 **`main`=`origin/main`**，**預設停在 `main`**（避免誤切功能分支）。若你**刻意**要沿用「對齊 main 後回到功能分支並 `merge origin/main`」舊流程：加 **`-FullMainlineFeature`**（或 **`-FullMainlineMainOnly:$false`**），並可用 **`-FullMainlineFeatureBranch <name>`** 指定分支；若要「找不到功能分支就硬失敗」用 **`-FullMainlineRequireFeatureBranch`**。見 **`REMOTE_WORKSTATION_STARTUP.md` §2.5.1**）。  
     **同一輪腳本內後續**（勿再加「第二套」完整檢查）：`fetch`（對齊段已 fetch 仍會再跑）；**僅 behind>0** 時 `git pull --ff-only origin main`；**`verify-build-gates`**；workflows 依賴；**`print-open-tasks`**；**`machine-environment-audit -FetchOrigin -Strict`**（見 **`docs/overview/REMOTE_WORKSTATION_STARTUP.md` 2.5.1**）。**不帶** `-FullMainlineParity` 時與 **`align-workstation.ps1`** 同行為（僅 preflight 軟對齊）。  
   - **僅在 exit code 0 後**，再進行本規則第 2 步讀檔與第 3 步回覆；**exit 非 0** 時依終端輸出處置（髒樹／衝突／Strict 缺項），**不得**宣稱已與 GitHub 對齊可開工大改。  
   - **待辦快照（與終端同源）**：`ao-resume.ps1` 預設在 **git preflight 之前**即跑 **`print-open-tasks`**，寫入 **`agency-os/.agency-state/open-tasks-snapshot.md`**（gitignore）。因此 **preflight 失敗（exit≠0）時，時間戳仍應接近本次執行**；待辦條目以該檔與 **`agency-os/TASKS.md`** 對齊為準。**第 2 步讀檔時必 Read 該檔**（若不存在則以 **`agency-os/TASKS.md`** 為準），用 **Total open** 與分段內容核對。**exit≠0 時不得**宣稱已完成 fetch／閘道／Strict 等後段步驟。  
   - **無法執行終端（極窄義 fallback）**：先確認是否僅為工作目錄錯誤——**必須** `cd` 到 monorepo 根再試一次 `ao-resume.ps1`。**仍**無 Shell 時，代理**自己**用其仍可用的方式完成 `fetch`／`status`／`rev-list` 等價檢查並在回覆標註「未跑完整 `ao-resume.ps1`」；**禁止**在下一輪已恢復終端時仍叫使用者代替代理重跑。  
   - **30 秒自檢（補強）**：腳本成功後可再跑 **REMOTE §2.3** 三指令；判讀 **ahead behind** 時 **behind 必須為 0**，**ahead>0** 不單獨阻斷（見 §2.3）。  
   - **Autopilot 開機**：**非**本節路徑——見 **`AGENTS.md`**（`-SkipVerify -SkipStrictEnvironmentAudit -AllowUnexpectedDirty`，輕量）。  
   - **GitHub／雙機**：遠端真相 **`origin/main`**；他機 **`AO-CLOSE` push** 後，**每一台要開工的 clone** 在開大改前須與 **`origin/main` 對齊**——在**當前機器、當前對話**上**優先**由代理執行 **`ao-resume.ps1`** 完成；**勿**把此句解讀成「使用者在聊天裡仍須另開終端再手動 pull 一次才算數」。

2. Read these files in order:
- `README.md`（根或 `agency-os` 視工作區而定）
- `agency-os/AGENTS.md`
- `agency-os/memory/CONVERSATION_MEMORY.md`
- `agency-os/memory/daily/` today and yesterday notes if present
- **`agency-os/.agency-state/open-tasks-snapshot.md`**（`ao-resume` 開頭即更新；**exit 0 時必讀**；exit≠0 時若需列待辦亦可讀，但不得宣稱 Strict／sync 已完成）
- `agency-os/TASKS.md`
- `agency-os/WORKLOG.md`
- `agency-os/LAST_SYSTEM_STATUS.md` (if exists)
- `lobster-factory/docs/LOBSTER_FACTORY_MASTER_CHECKLIST.md` (if exists)
- `lobster-factory/docs/LOBSTER_FACTORY_COMPLETION_PLAN_V2.md` (if exists)

If opened folder is `agency-os` only, use paths as above; lobster paths are `../lobster-factory/docs/...`.（**僅開 agency-os 時**：`.agency-state/open-tasks-snapshot.md` 路徑同左，無 `agency-os/` 前綴。）

3. **回覆結構（`AO-RESUME` 專用；禁止「過度精簡」到省略待辦或風險）**  
   以下**五個頂層標題依序輸出**（繁中），每節都須有**可讀的實質內容**（禁止整節只剩一兩個字或「略」）：

   - **已完成** — 綜合 `memory`、`WORKLOG` 近日、`LAST_SYSTEM_STATUS`（若存在）；若本輪沒有明確完工項，**明講**並指向 `TASKS`／`WORKLOG` 供對照。

   - **目前進度** — **必含**：
     - **龍蝦工廠**：**目前 Milestone**（M1~M5；不確定時寫「待對照 `LOBSTER_FACTORY_MASTER_CHECKLIST`」）；**今日 DoD**。
     - **阻塞／風險／需注意**：**禁止**只寫單獨「無」或「沒有」二字。須綜合 **`TASKS` 開放項**、`WORKLOG`、`CONVERSATION_MEMORY`、`LAST_SYSTEM_STATUS`（若存在）、`LOBSTER_FACTORY_MASTER_CHECKLIST`／Completion Plan 開放語意、**當輪** `ao-resume.ps1`／Strict 稽核終端輸出（若代理有看到）。若有阻塞或風險：**分點**列出（含建議下一步或需使用者決策處）。若盤點後確無：**寫「本次盤點：未見需立即處理之阻塞」**，並**至少一句**說明依據（例如 Strict 結果、WORKLOG／memory 是否曾標記風險）。
     - **Git／開工裁決** — **必含**（以當輪 **`ao-resume.ps1` 終端輸出**為準，禁止憑記憶编造）：
       - **`.\scripts\ao-resume.ps1`**：**exit code**（僅 **0** 得寫「預設完整開工路徑已跑完」）。
       - **Strict 環境稽核**：是否出現 **`AUDIT RESULT: PASS (no warnings)`**（來自同次執行之 **`machine-environment-audit -FetchOrigin -Strict`**）。若腳本非 0 或無該行，**不得**宣稱 Strict PASS；**至少摘一行**失敗原因（例：工作樹 dirty、`ahead>0` 未 push、behind 需 pull、閘道／health 失敗）。
       - **與 `origin/main`**：**`ahead`／`behind`** 數字或等價敘述（開工大改前 **behind 必須為 0**）。**註**：`machine-environment-audit -Strict` 會把多數 WARN（含常見的 **dirty**、**ahead>0**）視為 FAIL；若與 **`REMOTE_WORKSTATION_STARTUP.md` §2.3**「ahead 敘述」並存，**以稽核腳本實際輸出為準**。
       - 若未跑腳本或 exit 非 0：明寫「未跑／exit ≠ 0」，並指 **`docs/overview/REMOTE_WORKSTATION_STARTUP.md` §2.5.1** 的修復方向（commit／push／pull／`-AllowUnexpectedDirty` 等依終端 quick-fix）。

   - **未完成待辦（TASKS，`- [ ]` 全列）** — **必做**：以 **`open-tasks-snapshot.md`**（優先）與 **`agency-os/TASKS.md`** 對齊後，在聊天內**逐條**列出**每一行**仍為 `- [ ]` 的待辦，**完整複製該行原文**（checkbox 與文字均保留）；並標註其 **`##` 區塊**（與快照分段一致）。**禁止**只寫「見 TASKS」「共 N 條」而不列內文。**若** **Total open = 0**：寫「（0 條未完成）」**並**一句話確認。**條數必須**與快照／`print-open-tasks` **Total open** 一致（不一致須說明原因）。**若 Total open > 35**：仍**不得省略**任一行——可分段輸出（同一對話回合內接續貼上直至列完），並在首段註明「待辦較多，分 N 段列完」。**未全列 `- [ ]` 視為無效回覆，必須重出**。

   - **其他提醒（選填）** — `integrated-status-LATEST`、當日 `memory/daily`、或 `CONVERSATION_MEMORY` 中需接續的**非 TASKS 行項目**；若無則省略本節標題。

   - **下一步** — 具體、可執行；並保留下方第 6、7 點之雙機／筆電提醒（若適用）。

4. In `已完成` 或 `目前進度` 中：若本輪 **`ao-resume.ps1` 已執行且 exit 0**，**只**用終端輸出標註 **Git／Strict／ahead-behind**，**禁止**再請使用者「請你再跑一次 `ao-resume.ps1`」；僅在**本輪未跑**或 **exit≠0** 時，由代理在**同一或下一回合**自跑至成功或明確報告阻塞（無終端／需人類決策）。

5. Ask one focused confirmation question only if a blocker or ambiguity exists.

6. **雙機 §1.5 + audit（僅「另一台實體機」人類操作面）**：讀過 `TASKS.md` 後，若 **「（AO-RESUME 提醒）雙機環境對齊」** 仍為 **未勾選**，則 **「下一步」**（必要時 **「目前進度」**）**必含一行**說明：**另一台電腦**須依 **`docs/overview/REMOTE_WORKSTATION_STARTUP.md` §1.5** 完成首次接線；在**該台機器**的 monorepo 根，**優先**由**該台**上的代理／終端執行 `machine-environment-audit.ps1 -FetchOrigin -Strict` 至 **PASS（無 WARN）**。**在當前機器、當前 repo**：凡代理能執行 audit／腳本者**不得**改寫成「請使用者離開 Cursor 自行操作才算」。

7. **Windows 筆電／本機 WordPress（MariaDB）**：若 **「雙機環境對齊」** 仍 **未勾選**，或 `memory/CONVERSATION_MEMORY.md` 已記載**筆電要補**真 WP 相容層，則 **「下一步」** 除第 6 點外 **須再加一句**指 **`docs/overview/REMOTE_WORKSTATION_STARTUP.md` §1.5.1** 與 **`lobster-factory/docs/operations/LOCAL_WORDPRESS_WINDOWS.md`**（MariaDB／PHP／WP-CLI bootstrap）。**若當前開著的 repo 就在該筆電且代理具 elevated／套件安裝能力**，由代理**盡力自動執行**可腳本化步驟；**僅**在 OS 權限不允許無人值守安裝時，才列「需人類點一次 UAC／winget」之最小差異。與 MCP 分工：`docs/operations/cursor-mcp-and-plugin-inventory.md` §4。
