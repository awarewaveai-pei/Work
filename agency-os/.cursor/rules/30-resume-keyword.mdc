---
description: Resume session using AO-RESUME keyword
alwaysApply: true
---

# AO Resume Keyword

When the user message contains `AO-RESUME`, before proposing any new implementation:

1. **單一開工閘道（聊天打 `AO-RESUME`＝跑滿 §2 體檢）**  
   - **Agent 必做**：在**可執行終端**時，先解析 **monorepo 根**（`git rev-parse --show-toplevel`；若 cwd 非根則 `Set-Location` 到該根），再執行：  
     `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\ao-resume.ps1`  
     內含 **`check-three-way-sync -AutoFix`**：`git fetch`、**`HEAD`** 對齊 **`origin/main`**（必要時 stash 已知 noise／意外 dirty 後 **`git pull --ff-only origin main`**）、**`lobster-factory\packages\workflows`** 之 **`npm ci`**（若 repo 根存在 **`mcp-local-wrappers\package-lock.json`** 則同目錄 **`npm ci`**）、**`verify-build-gates`**，並更新 **`agency-os/.agency-state/ao-resume-last.txt`**。  
   - **腳本 exit 非 0**：禁止宣告「已對齊、可開工大改」；摘要錯誤、提示排解後重跑；**不要**在同一輪開始新實作。  
   - **無法執行終端**：回覆開頭說明原因，並請使用者在 monorepo 根**手動**跑**同一行**，成功後再打 `AO-RESUME`。  
   - **例外**：僅當使用者在**同一則訊息**明示要快跑、略過依賴時，才可改用 **`.\scripts\ao-resume.ps1 -SkipNpmCi`**；**預設不得**加 `-SkipNpmCi`。  
   - **腳本 PASS 後 30 秒自檢（仍必做）**：`git status -sb` + `git rev-list --left-right --count HEAD...origin/main`；須 **`0 0`** 且與「Latest + Working tree」結論一致，否則標風險。  
   - **Runbook 正本**：`agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md`（**§2** 逐步版與腳本等價；**§1.5** 新機；§2.3 自檢）。

2. Read these files in order:
- `README.md`（根或 `agency-os` 視工作區而定）
- `agency-os/AGENTS.md`
- `agency-os/memory/CONVERSATION_MEMORY.md`
- `agency-os/memory/daily/` today and yesterday notes if present
- `agency-os/TASKS.md`
- `agency-os/WORKLOG.md`
- `agency-os/LAST_SYSTEM_STATUS.md` (if exists)
- `lobster-factory/docs/LOBSTER_FACTORY_MASTER_CHECKLIST.md` (if exists)
- `lobster-factory/docs/LOBSTER_FACTORY_COMPLETION_PLAN_V2.md` (if exists)

If opened folder is `agency-os` only, use paths as above; lobster paths are `../lobster-factory/docs/...`.

3. Respond with a concise startup brief in this exact structure:
- 已完成
- 目前進度
- 下一步

4. In `目前進度`, always include **龍蝦工廠**欄位：
   - **目前 Milestone**（M1~M5）
   - **今日 DoD**
   - **阻塞／風險**（無則寫「無」）

5. In `已完成`, 必有一句標註 **`ao-resume.ps1` preflight** 為 **PASS** 或 **FAIL**（FAIL 時 `下一步` 只能列排解與重跑，不得列新功能）。

6. Ask one focused confirmation question only if a blocker or ambiguity exists.

7. **雙機 §1.5 + audit 口頭提醒**：讀過 `TASKS.md` 後，若 **「（AO-RESUME 提醒）雙機環境對齊」** 仍為 **未勾選**，則本次回覆的 **「下一步」**（必要時 **「目前進度」**）**必含一行**，明確提醒使用者：**另一台**須依 **`agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md` §1.5** 完成首次接線，並在 monorepo 根執行 `powershell -ExecutionPolicy Bypass -File .\scripts\machine-environment-audit.ps1 -FetchOrigin` 至 **PASS（無 WARN）** 後再勾該項。已勾選則無需重複。

8. **Windows 筆電／本機 WordPress（MariaDB）對齊**：若 **「雙機環境對齊」** 仍 **未勾選**，或 `agency-os/memory/CONVERSATION_MEMORY.md` 已記載使用者**桌機已裝、筆電要補**，則本次回覆 **「下一步」** 除第 7 點外 **須再加一句**，要求依 **`agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md` §1.5.1** 完成：**MariaDB.Server**、**PHP.PHP.NTS.8.4**、`scripts/setup-wp-cli-windows.ps1`、`scripts/bootstrap-local-wordpress-windows.ps1 -EnsurePhpIni`；並註明此為 **WordPress 專用 MySQL 相容層**，**非 MCP**、**不取代 Supabase**。操作正本：`lobster-factory/docs/operations/LOCAL_WORDPRESS_WINDOWS.md`；與 IDE MCP 分工對照：`agency-os/docs/operations/cursor-mcp-and-plugin-inventory.md` §4。
