---
description: End-of-day closeout before shutdown with linkage verification
alwaysApply: true
---

# Shutdown Closeout

**`AO-CLOSE` 關鍵字與收工回覆格式（今日完成／未完成／連動檢查／明日優先）維持不變。**  
目標：使用者收工前打 `AO-CLOSE` 後，**本機通過完整閘道、產出報告、並推上 GitHub**，公司電腦 **`git pull` 即得完整可續接狀態**（含龍蝦治理 + Agency 連動 + 綜合狀態檔）。

**給使用者（你只要這樣）**：輸入 **`AO-CLOSE`**（或「收工」「關機」等同等意圖）即可；**不必學** PowerShell 參數（例如 **`-CompletenessGate`**、**`-SkipPush`** 僅限代理除錯或你明示特例外由代理代打）。

**給代理（關鍵字 = 整包收工授權）**：收到 **`AO-CLOSE`** 後，**同一輪**內必須自行完成再呼叫腳本：**（1）**依 **`TASKS.md`**＋當輪可驗證事實，在當日 **`WORKLOG.md`** 補 **`- AUTO_TASK_DONE:`**（或已觸發 **`merge-closeout-inbox`** 所需之協作素材在 inbox）；**（2）必須**：依 **`10-memory-maintenance.mdc`** 更新 **`agency-os/memory/CONVERSATION_MEMORY.md`**（精簡蒸餾即可；**操作者預設不寫長篇**；inbox merge 的**指標列**由腳本追加，**不取代**此敘事）；**（3）**在 monorepo 根執行 **`powershell -ExecutionPolicy Bypass -File .\scripts\ao-close.ps1`** 至 **exit 0**；若 **`verify-closeout-completeness`** 擋下，**代理自修 WORKLOG 證據後重跑**，不要丟給使用者背指令。

**單一路徑（機械）**：**`ao-close.ps1`** 內含 scaffold、inbox guard、**`merge-closeout-inbox-into-progress`**（verbatim → **WORKLOG**／**daily**、**CONVERSATION_MEMORY** 指標、重置 inbox）、recap、閘道、**`apply-closeout-task-checkmarks`**、**`git add`**、**`verify-closeout-completeness`（預設 strict）**、commit／push。結尾會印 **`automation boundary`** 供對照。若 completeness **擋 commit**：**代理**補齊當日 **WORKLOG** 證據後**重跑**；操作者**不必**學 **`-CompletenessGate`**；僅你**明示特急**時由代理代下寬鬆旗標並在回覆**註明風險**。

**`AO-CLOSE` 關鍵字 = 內含授權**：**禁止**要求使用者再加一句「去寫 AUTO_TASK_DONE」才動手；證據不足時代理**只問一題**。

**多代理**：非收關者**只 append** **`agency-os/.agency-state/closeout-inbox.md`**；**`ao-close.ps1`** 會在開頭將可匯區塊 **verbatim** 併入 **`WORKLOG`**／**`memory/daily`**，並在 **`memory/CONVERSATION_MEMORY.md`** 的 **`## Current Operating Context`** 加**一則指標列**，然後自範本**重置 inbox**（見 **`docs/operations/collaborator-ai-agent-rules.md`**）。長 commit：**`-CommitMessageFile`**（**`end-of-day-checklist.md` §0.5**）。

When the user indicates end of day or shutdown intent (examples: `AO-CLOSE`,
"要關機", "收工", "今天先到這"), run this closeout sequence before ending:

1. **Before** invoking the script (same Cursor turn): 代理依 **`TASKS.md`**／當輪事實，在 **`WORKLOG.md`** 當日 **`## yyyy-MM-dd`** 補 **`- AUTO_TASK_DONE: <子字串>`**；並**必須**更新 **`agency-os/memory/CONVERSATION_MEMORY.md`**（**`10-memory-maintenance.mdc`**；人類不預設代筆長篇）。**日期**一律 **收工機器日**（與 **`print-today-closeout-recap`** 標題一致）。若記不得今日完成：可先跑 **`scripts/print-today-closeout-recap.ps1`** 對照；**`ao-close.ps1` 內也會再印 recap**。
2. Run **one-shot closeout + GitHub push** from the current repo root:
   `powershell -ExecutionPolicy Bypass -File .\scripts\ao-close.ps1`
   If `.\scripts\ao-close.ps1` is missing, retry:
   `powershell -ExecutionPolicy Bypass -File .\agency-os\scripts\ao-close.ps1`
   - **`ao-close.ps1` 內部順序**（與 Checklist **§1a** 一致；細節以腳本註解與原始碼為準）：
     1. **`ensure-daily-progress-scaffold.ps1`**（有則跑；補當日 **`WORKLOG` `##`**／**`memory/daily`** 檔；占位可為 `(TBD)`）
     2. **closeout inbox guard**（`-SkipInboxGuard`／`-InboxGuardMode`；預設 **warn**）
     3. **`merge-closeout-inbox-into-progress.ps1`**（有則跑；**verbatim** 併入 **WORKLOG**／**`memory/daily`**，並在 **`CONVERSATION_MEMORY.md`** 的 **`## Current Operating Context`** 下插入**指標列**（非全文重寫）；然後自範本重置 inbox）
     4. **`print-today-closeout-recap`**（`-SkipTodayRecap` 略過）
     5. （未 `-SkipPush`）**`git fetch`**；若落後 **`origin/<目前分支>`** → **中止**（`-AllowPushWhileBehind` 僅例外）
     6. **`verify-build-gates`**（`-SkipVerify` 略過；**不建議**跨機前略過）
     7. **`system-guard -Mode manual`**（內含 doc-sync + health + guard + `LAST_SYSTEM_STATUS.md`）
     8. **`generate-integrated-status-report`** → `reports/status/integrated-status-LATEST.md`
     9. 預設 health **100%**（`-AllowNonPerfectHealth` 僅明示核准）
     10. **`apply-closeout-task-checkmarks`**（`-SkipAutoTaskCheckmarks` 略過；讀 **WORKLOG `AUTO_TASK_DONE`** ± **pending 檔**）
     11. （未 `-SkipPush`）**`git add -A`** → **`verify-closeout-completeness.ps1`**（預設 **`-CompletenessGate strict`**；可 **`-SkipCompletenessGate`**）→ 有變更則 **`commit`** → **`git push origin <branch>`**
   - Execute from a folder that is the git repository root (or contains `agency-os/`).
3. Confirm：`verify-build-gates` 與 guard 路徑皆 **PASS**，且 push 成功（或依使用者要求正確使用 `-SkipPush`）。

**公司機／另一台續接**：`AO-CLOSE` push 成功後，他處開工依 `agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md`：**新機／筆電第一次 §1.5**。**之後每次（單一路徑）**：在 monorepo 根跑 **`scripts/ao-resume.ps1 -FullMainlineParity` 至 exit 0**（見 **`30-resume-keyword.mdc`** 與 **`REMOTE_WORKSTATION_STARTUP.md` §2.5.1**），再在 Cursor 打 **`AO-RESUME`**。**僅**需手動逐步理解或除錯時，才照 **§2** 分步。`node_modules` 不入庫。

**若 `git push` 被拒（遠端超前）**：在 repo 根執行 `git pull --rebase origin main`，解決衝突後 `git rebase --continue`，再推送。

Then respond with this exact structure:

- 今日完成:
  - <completed work>
- 今日未完成:
  - <pending work>
- 連動檢查:
  - <PASS/FAIL + key evidence>
- 明日優先:
  - P1
  - P2
  - P3

If any check fails, provide a rollback-safe fix plan before signing off.
